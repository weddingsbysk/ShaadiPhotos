import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <>
      <div>
        <h1>🎉 Shaadi Photos Site</h1>
        <p>Welcome to your new React + TypeScript + Vite website!</p>
      </div>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
        </button>
        <p>
          Edit <code>src/App.tsx</code> and save to test HMR
        </p>
      </div>
      <p className="read-the-docs">
        Your GitHub Pages site is ready to be deployed!
      </p>
    </>
  )
}

export default App
